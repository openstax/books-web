---
platform: linux

inputs:
- name: rex-web
- name: rex-web-build-branches
outputs:
- name: releases

image: docker

run:
  run:
  path: /bin/sh
  args:
    - -c
    - |
      apk add bash
      bash rex-web/concourse/create-multiple-releases/script.bash
